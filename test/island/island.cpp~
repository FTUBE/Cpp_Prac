#include <iostream>
#include <map>
#include <vector>
#include <cmath>

using namespace std;

void minimum_cost(vector<int> xi,vector<int> yi, int N);

int main(){

  return 0;
}


bool find_ele(vector<int> path, int tofind){
  int index = 0;
  while(index < path.size()){
    if(path[index] == tofind){
      return true;
    }
    index++;
  }
  return false;
}
